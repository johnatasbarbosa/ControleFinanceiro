@using Microsoft.AspNetCore.Identity

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User) && (User.IsInRole("Admin") || User.IsInRole("Secretary")))
{
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-controller="Aluno" asp-action="Index">
            <i class="nav-icon fas fa-th"></i>
            <p>
                Alunos
            </p>
        </a>
        <!-- <a href="#" class="nav-link">
        </a> -->
    </li>
}

@if (SignInManager.IsSignedIn(User) && (User.IsInRole("Admin")))
{
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-controller="Plano" asp-action="Index">
            <i class="nav-icon fas fa-th"></i>
            <p>
                Planos
            </p>
        </a>
        <!-- <a href="#" class="nav-link">
        </a> -->
    </li>
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-controller="Financa" asp-action="Index">
            <i class="nav-icon fas fa-th"></i>
            <p>
                Finanças
            </p>
        </a>
        <!-- <a href="#" class="nav-link">
        </a> -->
    </li>
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-controller="Log" asp-action="Index">
            <i class="nav-icon fas fa-th"></i>
            <p>
                Logs
            </p>
        </a>
        <!-- <a href="#" class="nav-link">
        </a> -->
    </li>
}
